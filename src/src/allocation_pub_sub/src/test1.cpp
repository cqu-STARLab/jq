// main.cpp
#include "cluster.h"

int main(int argc, char *argv[]) {
    // // 示例输入
    // std::vector<int> city_x = {1, 2, 3, 8, 8, 9, 20, 21};
    // std::vector<int> city_y = {2, 3, 4, 8, 8, 9, 20, 21};
    // std::vector<int> city_p0 = {10, 20, 15, 30, 35, 25, 50, 55};

    // double eps = 1.5;   // 邻域半径
    // int minPts = 2;     // 最小点数

    // // 创建 DBSCAN 对象并运行算法
    // DBSCAN dbscan(city_x, city_y, city_p0, eps, minPts);
    // dbscan.run();
    // dbscan.printClusters();

    return 0;
}
